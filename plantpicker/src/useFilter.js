/**hook to filter plants  based on:
 * water, 
 * flowering,
 * kid_friendly,
 * pet_friendly,
 * lighhting_condit
 * location_placement 
 */

import { useEffect, useState } from "react";

const useFilter = (data) => {
    const [quizData, setQuizData] = useState({})

    useEffect(() => {
        
    },[data])


}

export default useFilter;
